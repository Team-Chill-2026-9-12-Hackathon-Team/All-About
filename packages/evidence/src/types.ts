// Shared schema owner is B's contracts package. No local contract duplicate.
export type {
  AnswerBundle, AnswerBlock, BrowserBatch, Claim, Conflict, Coverage,
  DateValue, Evidence, KeyDate, PageSnapshot, QueryPlan, Scope,
} from "@allabout/contracts";
