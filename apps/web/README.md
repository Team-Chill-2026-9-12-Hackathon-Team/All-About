# AllAbout Campus — Web

Frontend for the AllAbout Campus web agent demo.

## Run locally

```bash
pnpm install
pnpm dev
```

## Checks

```bash
pnpm test
pnpm build
```

The current interface is a frontend demonstration. Search activity, citations, history, and browser views are simulated locally so the team can integrate real agent and Steel Browser APIs later.
